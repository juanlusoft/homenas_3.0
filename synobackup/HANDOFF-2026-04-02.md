# Handoff 2026-04-02

Estado al parar:

- agentes locales parados
- sesion `8890c133` marcada como `fail`
- `synobackup-core` sigue arriba en `http://192.168.1.81:3021`
- dispositivo de trabajo: `b6d071cd`

Lo que ya quedo bien:

- flujo base `VSS -> HTTP -> core`
- restore file-level
- inventario, diff, retencion y GC
- progreso en vivo
- `chunkBytes` ya se persiste correctamente en el inventario real
- fix del core para hash de archivos `> 2 GiB` por streaming en `core/src/routes.ts`
- perfil CDC calibrado en agente:
  - `128 KiB / 512 KiB / 2 MiB`

Pruebas reales realizadas:

1. Sesion `9e65d2af`
   - avanzo hasta `102250` ficheros y `26887107907` bytes logicos
   - fallo por `EOF` en un `PUT` al core
   - conclusion: problema transitorio de red/HTTP

2. Sesion `fc230516`
   - avanzo hasta `142179` ficheros y `33285478422` bytes logicos
   - fallo por `RangeError [ERR_FS_FILE_TOO_LARGE]`
   - causa: `sha256File()` en el core hacia `readFileSync()` sobre un fichero de `4269932544` bytes
   - fix ya aplicado: hash por streaming

3. Sesion `8890c133`
   - tercera pasada con reintentos en el agente
   - quedo colgada en estado `uploading`
   - ultimo progreso util:
     - `108085` ficheros
     - `29253124982` bytes
     - `updatedAt: 2026-04-01T22:30:34Z`
   - se paro manualmente para handoff

Observaciones importantes:

- el retry del agente para `EOF` y errores transitorios ya esta implementado en `agent/main.go`
- hubo dos binarios del agente conviviendo durante parte de la sesion:
  - `C:\Windows\Temp\synobackup-agent-windows-amd64.exe`
  - `synobackup/agent/dist/synobackup-agent-windows-amd64.exe`
- el binario que se estaba usando al final era el de `C:\Windows\Temp`
- hash del binario bueno recompilado en NAS:
  - `11df7863a1e719a9f9649c5012c3c67654fffd39fc990dfbf6c72adfe1d96250`

Punto pendiente real:

- entender por que la tercera pasada quedo colgada sin cerrar ni avanzar despues de `22:30`
- antes de seguir, asegurar un solo daemon/binario en Windows

Siguiente paso recomendado para manana:

1. limpiar por completo el agente local y dejar un solo binario
2. confirmar que el binario activo tiene hash `11df7863a1e719a9f9649c5012c3c67654fffd39fc990dfbf6c72adfe1d96250`
3. relanzar una sola pasada sobre `b6d071cd`
4. si vuelve a colgarse, dar un paso atras pragmatico:
   - desactivar temporalmente el camino chunked para archivos grandes
   - validar que el motor full file-level termina limpio
   - luego reintroducir el diferencial por chunks con menos variables a la vez

Decision tecnica abierta:

- si manana vuelve a romperse en el camino chunked, el paso atras correcto no es volver a SMB/robocopy
- el paso atras correcto es mantener `VSS -> HTTP -> core`, pero simplificar a upload por archivo completo para estabilizar el producto antes de reactivar CDC
